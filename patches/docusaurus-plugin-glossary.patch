diff --git a/dist/chunk-CIAA7XR3.js b/dist/chunk-CIAA7XR3.js
index 57169b64d0c4064adcb80ef2a2cbe7d131dff48b..c4c8e5f2c41b64abc0500e0a977d7c73656a11fb 100644
--- a/dist/chunk-CIAA7XR3.js
+++ b/dist/chunk-CIAA7XR3.js
@@ -153,15 +153,16 @@ function remarkGlossaryTerms({
         const afterIndex = index + needle.length;
         const afterChar = afterIndex < textLower.length ? textLower[afterIndex] : " ";
         let matchLength = needle.length;
-        let isWordBoundary = !/\w/.test(beforeChar) && !/\w/.test(afterChar);
-        if (!isWordBoundary && afterChar === "s") {
+        const startsAtBoundary = !/\w/.test(beforeChar);
+        let isWordBoundary = startsAtBoundary && !/\w/.test(afterChar);
+        if (startsAtBoundary && !isWordBoundary && afterChar === "s") {
           const nextChar = afterIndex + 1 < textLower.length ? textLower[afterIndex + 1] : " ";
           if (!/\w/.test(nextChar)) {
             isWordBoundary = true;
             matchLength = needle.length + 1;
           }
         }
-        if (!isWordBoundary && afterChar === "e" && afterIndex + 1 < textLower.length && textLower[afterIndex + 1] === "s") {
+        if (startsAtBoundary && !isWordBoundary && afterChar === "e" && afterIndex + 1 < textLower.length && textLower[afterIndex + 1] === "s") {
           const nextChar = afterIndex + 2 < textLower.length ? textLower[afterIndex + 2] : " ";
           if (!/\w/.test(nextChar)) {
             isWordBoundary = true;
diff --git a/dist/chunk-EQBYCLUI.js b/dist/chunk-EQBYCLUI.js
index 76c927e1e187095498a2f9d6014c840fa6b5abfc..127ec78925a127e288ebf0efd3a2b66fcb8b9a64 100644
--- a/dist/chunk-EQBYCLUI.js
+++ b/dist/chunk-EQBYCLUI.js
@@ -63,14 +63,18 @@ function glossaryPlugin(context, options = {}) {
           routePath
         })
       );
-      addRoute({
-        path: routePath,
-        component: path.join(currentDir, "components/GlossaryPage.js"),
-        exact: true,
-        modules: {
-          glossaryData: glossaryDataPath
-        }
-      });
+      // routePath: null lets a site render the glossary itself, in a page of its
+      // own, without this plugin also registering one at the same path.
+      if (routePath !== null) {
+        addRoute({
+          path: routePath,
+          component: path.join(currentDir, "components/GlossaryPage.js"),
+          exact: true,
+          modules: {
+            glossaryData: glossaryDataPath
+          }
+        });
+      }
       setGlobalData({
         terms: glossaryContent.terms || [],
         routePath
diff --git a/dist/index.cjs b/dist/index.cjs
index 0428381d07d043b7577a9e846e86c6f1a595c772..08cedc713828fed2a1754a9c06185d53e36234d4 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -205,15 +205,16 @@ function remarkGlossaryTerms({
         const afterIndex = index + needle.length;
         const afterChar = afterIndex < textLower.length ? textLower[afterIndex] : " ";
         let matchLength = needle.length;
-        let isWordBoundary = !/\w/.test(beforeChar) && !/\w/.test(afterChar);
-        if (!isWordBoundary && afterChar === "s") {
+        const startsAtBoundary = !/\w/.test(beforeChar);
+        let isWordBoundary = startsAtBoundary && !/\w/.test(afterChar);
+        if (startsAtBoundary && !isWordBoundary && afterChar === "s") {
           const nextChar = afterIndex + 1 < textLower.length ? textLower[afterIndex + 1] : " ";
           if (!/\w/.test(nextChar)) {
             isWordBoundary = true;
             matchLength = needle.length + 1;
           }
         }
-        if (!isWordBoundary && afterChar === "e" && afterIndex + 1 < textLower.length && textLower[afterIndex + 1] === "s") {
+        if (startsAtBoundary && !isWordBoundary && afterChar === "e" && afterIndex + 1 < textLower.length && textLower[afterIndex + 1] === "s") {
           const nextChar = afterIndex + 2 < textLower.length ? textLower[afterIndex + 2] : " ";
           if (!/\w/.test(nextChar)) {
             isWordBoundary = true;
@@ -710,14 +711,18 @@ function glossaryPlugin(context, options = {}) {
           routePath
         })
       );
-      addRoute({
-        path: routePath,
-        component: import_path2.default.join(currentDir, "components/GlossaryPage.js"),
-        exact: true,
-        modules: {
-          glossaryData: glossaryDataPath
-        }
-      });
+      // routePath: null lets a site render the glossary itself, in a page of its
+      // own, without this plugin also registering one at the same path.
+      if (routePath !== null) {
+        addRoute({
+          path: routePath,
+          component: import_path2.default.join(currentDir, "components/GlossaryPage.js"),
+          exact: true,
+          modules: {
+            glossaryData: glossaryDataPath
+          }
+        });
+      }
       setGlobalData({
         terms: glossaryContent.terms || [],
         routePath
diff --git a/dist/preset.cjs b/dist/preset.cjs
index 5626528e696fdc5cbd9186d262b246c7c8e32dd0..d9052bd40cc8509869f24bb786ec40fd70271e6e 100644
--- a/dist/preset.cjs
+++ b/dist/preset.cjs
@@ -199,15 +199,16 @@ function remarkGlossaryTerms({
         const afterIndex = index + needle.length;
         const afterChar = afterIndex < textLower.length ? textLower[afterIndex] : " ";
         let matchLength = needle.length;
-        let isWordBoundary = !/\w/.test(beforeChar) && !/\w/.test(afterChar);
-        if (!isWordBoundary && afterChar === "s") {
+        const startsAtBoundary = !/\w/.test(beforeChar);
+        let isWordBoundary = startsAtBoundary && !/\w/.test(afterChar);
+        if (startsAtBoundary && !isWordBoundary && afterChar === "s") {
           const nextChar = afterIndex + 1 < textLower.length ? textLower[afterIndex + 1] : " ";
           if (!/\w/.test(nextChar)) {
             isWordBoundary = true;
             matchLength = needle.length + 1;
           }
         }
-        if (!isWordBoundary && afterChar === "e" && afterIndex + 1 < textLower.length && textLower[afterIndex + 1] === "s") {
+        if (startsAtBoundary && !isWordBoundary && afterChar === "e" && afterIndex + 1 < textLower.length && textLower[afterIndex + 1] === "s") {
           const nextChar = afterIndex + 2 < textLower.length ? textLower[afterIndex + 2] : " ";
           if (!/\w/.test(nextChar)) {
             isWordBoundary = true;
@@ -697,14 +698,18 @@ function glossaryPlugin(context, options = {}) {
           routePath
         })
       );
-      addRoute({
-        path: routePath,
-        component: import_path2.default.join(currentDir, "components/GlossaryPage.js"),
-        exact: true,
-        modules: {
-          glossaryData: glossaryDataPath
-        }
-      });
+      // routePath: null lets a site render the glossary itself, in a page of its
+      // own, without this plugin also registering one at the same path.
+      if (routePath !== null) {
+        addRoute({
+          path: routePath,
+          component: import_path2.default.join(currentDir, "components/GlossaryPage.js"),
+          exact: true,
+          modules: {
+            glossaryData: glossaryDataPath
+          }
+        });
+      }
       setGlobalData({
         terms: glossaryContent.terms || [],
         routePath
diff --git a/dist/remark/glossary-terms.cjs b/dist/remark/glossary-terms.cjs
index 289d45dbccad081e548d336df2c31745c4f3cdaa..6ddcf07a57705038ae9ecaed6e332d0b93bbedd6 100644
--- a/dist/remark/glossary-terms.cjs
+++ b/dist/remark/glossary-terms.cjs
@@ -188,15 +188,16 @@ function remarkGlossaryTerms({
         const afterIndex = index + needle.length;
         const afterChar = afterIndex < textLower.length ? textLower[afterIndex] : " ";
         let matchLength = needle.length;
-        let isWordBoundary = !/\w/.test(beforeChar) && !/\w/.test(afterChar);
-        if (!isWordBoundary && afterChar === "s") {
+        const startsAtBoundary = !/\w/.test(beforeChar);
+        let isWordBoundary = startsAtBoundary && !/\w/.test(afterChar);
+        if (startsAtBoundary && !isWordBoundary && afterChar === "s") {
           const nextChar = afterIndex + 1 < textLower.length ? textLower[afterIndex + 1] : " ";
           if (!/\w/.test(nextChar)) {
             isWordBoundary = true;
             matchLength = needle.length + 1;
           }
         }
-        if (!isWordBoundary && afterChar === "e" && afterIndex + 1 < textLower.length && textLower[afterIndex + 1] === "s") {
+        if (startsAtBoundary && !isWordBoundary && afterChar === "e" && afterIndex + 1 < textLower.length && textLower[afterIndex + 1] === "s") {
           const nextChar = afterIndex + 2 < textLower.length ? textLower[afterIndex + 2] : " ";
           if (!/\w/.test(nextChar)) {
             isWordBoundary = true;
diff --git a/dist/theme/GlossaryTerm/index.cjs b/dist/theme/GlossaryTerm/index.cjs
index 88a801adfa97e7c74064ad8113f0cdb31d077b26..ed286b316b6b6d507a79d5c0d96aa523439a64d8 100644
--- a/dist/theme/GlossaryTerm/index.cjs
+++ b/dist/theme/GlossaryTerm/index.cjs
@@ -35,6 +35,7 @@ __export(GlossaryTerm_exports, {
 module.exports = __toCommonJS(GlossaryTerm_exports);
 var import_react = __toESM(require("react"), 1);
 var import_useGlobalData = require("@docusaurus/useGlobalData");
+var import_useBaseUrl = __toESM(require("@docusaurus/useBaseUrl"), 1);
 var import_styles = __toESM(require("../../styles.module-QQW7ISLV.module.css"), 1);
 var import_jsx_runtime = require("react/jsx-runtime");
 function GlossaryTerm({
@@ -56,7 +57,9 @@ function GlossaryTerm({
     const viewportWidth = window.innerWidth;
     const viewportHeight = window.innerHeight;
     const preferredGap = 8;
-    const hasSpaceAbove = wrapperRect.top >= tooltipRect.height + preferredGap;
+    const stickyHeader = document.querySelector(".navbar");
+    const topBoundary = stickyHeader ? stickyHeader.getBoundingClientRect().bottom : 0;
+    const hasSpaceAbove = wrapperRect.top - topBoundary >= tooltipRect.height + preferredGap;
     const hasSpaceBelow = viewportHeight - wrapperRect.bottom >= tooltipRect.height + preferredGap;
     const nextPlacement = hasSpaceAbove || !hasSpaceBelow ? "top" : "bottom";
     let top;
@@ -72,7 +75,7 @@ function GlossaryTerm({
       Math.min(left, viewportWidth - tooltipRect.width - horizontalMargin)
     );
     setPlacement(nextPlacement);
-    setTooltipStyle({ top: Math.max(4, top), left });
+    setTooltipStyle({ top: Math.max(topBoundary + 4, top), left });
   }, []);
   (0, import_react.useEffect)(() => {
     if (!showTooltip) return;
@@ -122,13 +125,22 @@ function GlossaryTerm({
     if (routePath && typeof routePath === "string" && routePath.length > 0) return routePath;
     return pluginData && pluginData.routePath || "/glossary";
   }, [pluginData, routePath]);
+  // A term with a `link` sends the click to its own page instead of the glossary entry.
+  const termLink = (0, import_react.useMemo)(() => {
+    const terms = pluginData && pluginData.terms || [];
+    const found = terms.find(
+      (t) => typeof t.term === "string" && t.term.toLowerCase() === String(term).toLowerCase()
+    );
+    return found && typeof found.link === "string" && found.link.length > 0 ? found.link : void 0;
+  }, [pluginData, term]);
+  const termLinkHref = (0, import_useBaseUrl.default)(termLink || "");
   const displayText = children || term;
   const termId = term.toLowerCase().replace(/\s+/g, "-");
   return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { ref: wrapperRef, className: import_styles.default.glossaryTermWrapper, children: [
     /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
       "a",
       {
-        href: `${effectiveRoutePath}#${termId}`,
+        href: termLink ? termLinkHref : `${effectiveRoutePath}#${termId}`,
         className: import_styles.default.glossaryTerm,
         onMouseEnter: () => setShowTooltip(true),
         onMouseLeave: () => setShowTooltip(false),
diff --git a/dist/theme/GlossaryTerm/index.js b/dist/theme/GlossaryTerm/index.js
index 0eb7ea5e87d149d1165073c0179333e2ca3d1a38..2c546afc5f1f9c181fd443f00b494789a2ca5123 100644
--- a/dist/theme/GlossaryTerm/index.js
+++ b/dist/theme/GlossaryTerm/index.js
@@ -1,6 +1,7 @@
 // src/theme/GlossaryTerm/index.js
 import React, { useMemo, useState, useRef, useEffect, useCallback } from "react";
 import { usePluginData } from "@docusaurus/useGlobalData";
+import useBaseUrl from "@docusaurus/useBaseUrl";
 import styles from "../../styles.module-QQW7ISLV.module.css";
 import { jsx, jsxs } from "react/jsx-runtime";
 function GlossaryTerm({
@@ -22,7 +23,9 @@ function GlossaryTerm({
     const viewportWidth = window.innerWidth;
     const viewportHeight = window.innerHeight;
     const preferredGap = 8;
-    const hasSpaceAbove = wrapperRect.top >= tooltipRect.height + preferredGap;
+    const stickyHeader = document.querySelector(".navbar");
+    const topBoundary = stickyHeader ? stickyHeader.getBoundingClientRect().bottom : 0;
+    const hasSpaceAbove = wrapperRect.top - topBoundary >= tooltipRect.height + preferredGap;
     const hasSpaceBelow = viewportHeight - wrapperRect.bottom >= tooltipRect.height + preferredGap;
     const nextPlacement = hasSpaceAbove || !hasSpaceBelow ? "top" : "bottom";
     let top;
@@ -38,7 +41,7 @@ function GlossaryTerm({
       Math.min(left, viewportWidth - tooltipRect.width - horizontalMargin)
     );
     setPlacement(nextPlacement);
-    setTooltipStyle({ top: Math.max(4, top), left });
+    setTooltipStyle({ top: Math.max(topBoundary + 4, top), left });
   }, []);
   useEffect(() => {
     if (!showTooltip) return;
@@ -88,13 +91,22 @@ function GlossaryTerm({
     if (routePath && typeof routePath === "string" && routePath.length > 0) return routePath;
     return pluginData && pluginData.routePath || "/glossary";
   }, [pluginData, routePath]);
+  // A term with a `link` sends the click to its own page instead of the glossary entry.
+  const termLink = useMemo(() => {
+    const terms = pluginData && pluginData.terms || [];
+    const found = terms.find(
+      (t) => typeof t.term === "string" && t.term.toLowerCase() === String(term).toLowerCase()
+    );
+    return found && typeof found.link === "string" && found.link.length > 0 ? found.link : void 0;
+  }, [pluginData, term]);
+  const termLinkHref = useBaseUrl(termLink || "");
   const displayText = children || term;
   const termId = term.toLowerCase().replace(/\s+/g, "-");
   return /* @__PURE__ */ jsxs("span", { ref: wrapperRef, className: styles.glossaryTermWrapper, children: [
     /* @__PURE__ */ jsx(
       "a",
       {
-        href: `${effectiveRoutePath}#${termId}`,
+        href: termLink ? termLinkHref : `${effectiveRoutePath}#${termId}`,
         className: styles.glossaryTerm,
         onMouseEnter: () => setShowTooltip(true),
         onMouseLeave: () => setShowTooltip(false),
